# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

ecom_agent - 电商自动化代理工具（待定义具体功能）

## Development Framework

本项目遵循父目录 `D:\AI\CLAUDE.md` 定义的 AI Agent 协作框架：

- **阶段流程**: req → design → dev → test
- **阶段管理**: 使用 `/stage` 命令切换阶段
- **Plan 模式**: req/design 阶段禁止写代码，仅产出文档

## Project Status

**当前状态**: 项目初始化，尚未开始需求阶段

**下一步**: 执行 `/stage init` 初始化阶段文件夹结构，然后 `/stage req` 进入需求阶段
